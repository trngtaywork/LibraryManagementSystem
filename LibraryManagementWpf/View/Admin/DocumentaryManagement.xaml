<UserControl x:Class="LibraryManagementWpf.View.Admin.DocumentaryManagement"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagementWpf.View.Admin"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080" DataContext="{StaticResource docAdminVM}">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#eff2f7" Margin="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="350" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <!-- Header -->
                <TextBlock Text="Quản lý đồ án tốt nghiệp" FontSize="20" FontWeight="Bold" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" />

                <ComboBox x:Name="txtFilter" Width="135" Margin="0,10,231,0" VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource ComboBoxFlatStyle}" SelectedValue="{Binding SelectedFilterOption, Mode=TwoWay}" Height="35">
                    <ComboBox.ItemsSource>
                        <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                            <sys:String>All</sys:String>
                            <sys:String>Mới nhất</sys:String>
                            <sys:String>Cũ nhất</sys:String>
                        </x:Array>
                    </ComboBox.ItemsSource>
                </ComboBox>
                <!--Search TextBox-->
                <Grid  Width="202" VerticalAlignment="Top" HorizontalAlignment="Right" Height="50" Margin="0,0,-5,0">
                    <TextBox x:Name="txtSearch" Style="{StaticResource textboxSearch}" Margin="-17,0,18,0" Text="{Binding Keyword}" Height="38"/>

                    <TextBlock Panel.ZIndex="1" Text="Search here ..." 
                       Visibility="{Binding ElementName=txtSearch, Path=Text.IsEmpty, Converter={StaticResource BoolToVis}}" 
                       Margin="0,0,101,0" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Height="17"/>

                    <Button Command="{Binding SearchCommand}" Style="{x:Null}" Cursor="Hand"
                        VerticalAlignment="Center" HorizontalAlignment="Right"
                        Width="30" Height="30" Margin="0,0,35,0" Background="Transparent" BorderBrush="Transparent">
                        <Icon:PackIconMaterial Kind="Magnify"
                         Foreground="#b0b9c6" Margin="0 0 0 0"/>
                    </Button>
                </Grid>

                <!-- Thesis Management Table -->
                <DataGrid x:Name="ThesisDataGrid" ItemsSource="{Binding DisplayedListPerPage}" AutoGenerateColumns="False" Margin="10,50,10,98" IsReadOnly="True" SelectedItem="{Binding SelectedThesis}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Visibility="Collapsed" Binding="{Binding Id}" />
                        <DataGridTemplateColumn Header="Tiêu đề" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Người thực hiện" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Author}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Tài liệu" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding FileDoc}" Value="">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    <Hyperlink NavigateUri="{Binding FileDoc}" RequestNavigate="Hyperlink_RequestNavigate">
                                        <Run Text="Download Document" />
                                    </Hyperlink>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Ngày tạo" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CreateAt, StringFormat='dd/MM/yyyy'}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <!-- Edit Button -->
                        <DataGridTemplateColumn Header="Chỉnh sửa" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource gridEditButton}" 
                                         Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                         CommandParameter="{Binding}">
                                        <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Delete Button -->
                        <DataGridTemplateColumn Header="Xóa" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource gridRemoveButton}" Margin="5 0 0 0"  
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                        <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel HorizontalAlignment="Center" Margin="0,602,0,10" Width="155">
                    <Border CornerRadius="5" Padding="5" Background="White" Margin="0 10 10 10" Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#dee4ec" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource paginButton}">
                                <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource paginButtonIcon}"/>
                            </Button>

                            <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                            <Button Style="{StaticResource paginButton}"  Background="#7950f2" Foreground="#ffffff">
                                <TextBlock>
                                  <Run Text="{Binding CurrentPage}" />
                                  <Run Text="/" />
                                  <Run Text="{Binding TotalPages, Mode=OneWay}" />
                                </TextBlock>
                            </Button>
                            
                            <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                            <Button Command="{Binding NextPageCommand}" Style="{StaticResource paginButton}">
                                <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource paginButtonIcon}"/>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1" Background="WhiteSmoke">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Thông tin đồ án" FontSize="20" FontWeight="Bold" Margin="0,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Row="1"/>

                <!-- Input Fields for Thesis Details -->
                <StackPanel Margin="10" Grid.Row="2">
                    <TextBox Visibility="Collapsed" Background="White" Width="300" Margin="0,5,0,10" Text="{Binding Id}" />

                    <TextBlock Text="Tiêu đề:"/>
                    <TextBox Background="White" Width="300" Margin="0,5,0,10" Text="{Binding Title}" />
                    <TextBox Background="WhiteSmoke" IsReadOnly="True" Text="{Binding TitleError}" Width="300" Margin="0,5,0,10" Foreground="Red"/>

                    <TextBlock Text="Người thực hiện:"/>
                    <TextBox Background="White"  Width="300" Margin="0,5,0,10" Text="{Binding Author}" />
                    <TextBox Background="WhiteSmoke" IsReadOnly="True" Text="{Binding AuthorError}" Width="300" Margin="0,5,0,10" Foreground="Red"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Chọn file tài liệu:"  Margin="0,10,20,10"/>
                        <Button Content="Chọn file" Command="{Binding SelectFileCommand}" Style="{StaticResource gridEditButton}" Width="100"/>
                    </StackPanel>
                    <TextBox Background="WhiteSmoke" IsReadOnly="True" Text="{Binding SelectFileError}" Width="300" Margin="0,5,0,10" Foreground="Red"/>

                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="3" Margin="0,10,0,0">
                    <Button Command="{Binding SaveCommand}" Content="Lưu đồ án" Style="{StaticResource buttonMainGreen}" Margin="5"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>