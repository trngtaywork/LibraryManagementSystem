<UserControl x:Class="LibraryManagementWpf.View.Admin.CreatePost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagementWpf.View.Admin"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{StaticResource PostVM}">
    <Border Background="#eff2f7" Margin="0 30 0 0">
        <Grid>
            <wv2:WebView2 x:Name="webview" Grid.Row="0" />

            <Button Command="{Binding AddCommand}" Content="Lưu bài viết" Grid.Row="1" VerticalAlignment="Top" Margin="10" Style="{StaticResource buttonMainGreen}"/>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
        </Grid>
    </Border>
</UserControl>
