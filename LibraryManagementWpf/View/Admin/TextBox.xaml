<UserControl x:Class="LibraryManagementWpf.View.Admin.TextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="textBox">

   <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    
    <Grid>
            <TextBox Text="{Binding Path=Hint, ElementName=textBox}" Foreground="Black" Margin="10 0" VerticalAlignment="Center" Panel.ZIndex="1"/>
    </Grid>
</UserControl>
