<UserControl x:Class="LibraryManagementWpf.View.Admin.PostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagementWpf.View.Admin"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="720" d:DesignWidth="1080" DataContext="{StaticResource PostVM}">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#eff2f7" Margin="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <!-- Header -->
                <TextBlock Text="Quản lý bài đăng" FontSize="20" FontWeight="Bold" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left" />

                <!-- Add Button -->
                <Button DataContext="{StaticResource AdminVM}" Command="{Binding AddPostViewCommand}" Style="{StaticResource addButton}" Width="150" Height="30" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                        <TextBlock Text="Thêm mới bài đăng"></TextBlock>
                    </StackPanel>
                </Button>

                <!-- Post Management Table -->
                <DataGrid x:Name="PostDataGrid" ItemsSource="{Binding DisplayedListPerPage}" AutoGenerateColumns="False" Margin="10,50,10,102" IsReadOnly="True">
                    <DataGrid.Columns>
                        <!-- Post Title Column -->
                        <DataGridTextColumn Binding="{Binding Id}" Visibility="Hidden"/>
                        <DataGridTextColumn Header="Tiêu đề bài viết" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt}" Width="150"/>

                        <!-- Edit Button -->
                        <DataGridTemplateColumn Header="Chỉnh sửa" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource gridEditButton}" 
                                             Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                             CommandParameter="{Binding}">
                                        <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Delete Button -->
                        <DataGridTemplateColumn Header="Xóa" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource gridRemoveButton}" Margin="5 0 0 0"  
                                            Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}">
                                        <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel HorizontalAlignment="Center" Margin="198,603,182,23">
                    <Border CornerRadius="5" Padding="5" Background="White" Margin="0 10 10 10" Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#dee4ec" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource paginButton}">
                                <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource paginButtonIcon}"/>
                            </Button>

                            <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                            <Button Style="{StaticResource paginButton}"  Background="#7950f2" Foreground="#ffffff">
                                <TextBlock>
                                 <Run Text="{Binding CurrentPage}" />
                                 <Run Text="/" />
                                 <Run Text="{Binding TotalPages, Mode=OneWay}" />
                                </TextBlock>
                            </Button>
                            
                            <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                            <Button Command="{Binding NextPageCommand}" Style="{StaticResource paginButton}">
                                <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource paginButtonIcon}"/>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Chỉnh sửa bài viết" FontSize="20" FontWeight="Bold" Margin="0,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Row="0"/>

                <wv2:WebView2 x:Name="webview" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,10,0,10" Grid.Row="1"/>

                <Button Command="{Binding DoEditCommand}" Content="Lưu bài viết" VerticalAlignment="Bottom" Margin="10" Style="{StaticResource buttonMainGreen}" HorizontalAlignment="Center" Grid.Row="2"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
