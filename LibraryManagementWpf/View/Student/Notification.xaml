<UserControl x:Class="LibraryManagementWpf.View.Student.Notification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagementWpf.View.Student"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{StaticResource NotifyVM}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="#f7f8fa">

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header Section -->
        <DockPanel Grid.Row="0" Grid.ColumnSpan="2" Height="100" Margin="10">
            <TextBlock Text="Bảng tin mới" FontSize="32" FontWeight="Bold" Foreground="#1a3d90" VerticalAlignment="Center" />

            <!-- Search Bar -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0,0,0">
                <Border CornerRadius="10" BorderBrush="#1a3d90" BorderThickness="1" Background="White">
                    <TextBox Background="White" Width="300" Padding="10,5" BorderThickness="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </Border>
                <Button Style="{StaticResource buttonMainGreen}" Command="{Binding SearchPostCommand}"  Width="80" Height="36" Margin="10,0,0,0" Background="#ff6b35" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold">
                    Tìm kiếm
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- List -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10" Grid.Row="1" Grid.Column="0">
            <ItemsControl ItemsSource="{Binding ListPosts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="10" Padding="15" Margin="5,10" BorderBrush="#dee4ec" BorderThickness="1">
                            <StackPanel Cursor="Hand">
                                <StackPanel.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#1a3d90"/>
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="#FF4500"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Resources>

                                <StackPanel.InputBindings>
                                    <MouseBinding Gesture="LeftClick"
                                              Command="{Binding DataContext.ViewPostDetailCommand, 
                                              RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                              CommandParameter="{Binding}"/>
                                </StackPanel.InputBindings>

                                <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <Separator/>
                                <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MMMM dd, yyyy}'}" FontSize="14" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- WebView2 Section -->
        <Border Grid.Row="1" Grid.Column="1" CornerRadius="15" Background="White">
            <wv2:WebView2 x:Name="webview"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10" />
        </Border>
    </Grid>
</UserControl>
