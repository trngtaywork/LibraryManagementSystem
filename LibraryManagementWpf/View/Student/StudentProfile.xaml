<UserControl x:Class="LibraryManagementWpf.View.Student.StudentProfile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources >
    <DockPanel Margin="20" Background="#eff2f7">
        <DockPanel DockPanel.Dock="Top" Margin="10">
            <TextBlock Text="Student Board" FontWeight="Bold" FontSize="25" Foreground="#1E73BE" Margin="0,0,0,20"/>
        </DockPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3.5*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="8*" />
                </Grid.RowDefinitions>

                <Border CornerRadius="10" Grid.Row="0" Background="#1E73BE" Margin="0,0,0,5">
                    <Border.Effect>
                        <DropShadowEffect Direction="225" ShadowDepth="6" Color="Black" Opacity="0.4"/>
                    </Border.Effect>
                    <TextBlock Text="Tin tức" VerticalAlignment="Center" FontWeight="Bold" FontSize="18" Foreground="White" Margin="20,0,0,0"></TextBlock>
                </Border>

                <Border CornerRadius="10" Grid.Row="1" Background="White" Margin="0,10,0,0">
                    <Border.Effect>
                        <DropShadowEffect Direction="225" ShadowDepth="6" Color="Black" Opacity="0.4"/>
                    </Border.Effect>
                    <ListView x:Name="NewsList" BorderThickness="0" Margin="10">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,5,0,5">
                                    <TextBlock FontWeight="Bold" Foreground="DarkBlue" Text="{Binding Title}" />
                                    <TextBlock Foreground="Gray" FontSize="12" >
                                        <Run Text="Admin" />
                                        <Run Text=" - "/>
                                        <Run Text="{Binding CreatedAt}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="5*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="3*" />
                </Grid.RowDefinitions>
                <Border CornerRadius="10" Grid.Row="0" Background="#1E73BE" Margin="0,0,0,5">
                    <Border.Effect>
                        <DropShadowEffect Direction="225" ShadowDepth="6" Color="Black" Opacity="0.4"/>
                    </Border.Effect>
                    <TextBlock Text="Thông tin cá nhân" VerticalAlignment="Center" FontWeight="Bold" FontSize="18" Foreground="White" Margin="20,0,0,0"></TextBlock>
                </Border>
                <Border CornerRadius="10" Grid.Row="1" Background="White" Margin="0,10,0,10">
                    <Border.Effect>
                        <DropShadowEffect Direction="225" ShadowDepth="6" Color="Black" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Image}" Width="150" Margin="30" ClipToBounds="True" Stretch="Uniform" />
                        <StackPanel Orientation="Vertical" Margin="0,30,0,0">
                            <TextBlock Text="{Binding Fullname}" Foreground="DarkBlue" FontWeight="Bold" FontSize="25" />
                            <TextBlock Text="{Binding StudentCode}" FontSize="18"/>
                            <TextBlock FontSize="16">
                                <Run Text="Phone: " Foreground="DarkBlue" />
                                <Run Text="{Binding Phone}"/>
                            </TextBlock>
                            <TextBlock FontSize="16">
                                 <Run Text="Mail: " Foreground="DarkBlue" />
                                 <Run Text="{Binding Email}"/>
                            </TextBlock>
                            
                            <Button Command="{Binding DataContext.UpdateProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Content="Update Profile" Width="120" Height="40" Margin="0,10,10,0" Background="#4CAF50" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>

                    </StackPanel>
                </Border>
                <Border CornerRadius="10" Grid.Row="2" Background="#1E73BE" Margin="0,0,0,5">
                    <Border.Effect>
                        <DropShadowEffect Direction="225" ShadowDepth="6" Color="Black" Opacity="0.4"/>
                    </Border.Effect>
                    <TextBlock Text="Liên hệ" VerticalAlignment="Center" FontWeight="Bold" FontSize="18" Foreground="White" Margin="20,0,0,0"></TextBlock>
                </Border>
                <Border CornerRadius="10" Grid.Row="3" Background="White" Margin="0,10,0,0">
                    <Border.Effect>
                        <DropShadowEffect Direction="225" ShadowDepth="6" Color="Black" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock FontSize="18" Margin="10,5,0,10" >
                            <Run Text="Security room: " Foreground="DarkBlue" FontWeight="Bold"/>
                            <Run Text="(024) 6680 5 913"  />
                        </TextBlock>
                        <TextBlock FontSize="18" Margin="10,5,0,10" >
                            <Run Text="Health station: " Foreground="DarkBlue" FontWeight="Bold"/>
                            <Run Text="(024) 6680 5 917" />
                        </TextBlock>
                        <TextBlock FontSize="18" Margin="10,5,0,10" >
                            <Run Text="Student service: "  Foreground="DarkBlue" FontWeight="Bold"/>
                            <Run Text="(024) 7308 1313"/>
                        </TextBlock>
                        <TextBlock FontSize="18" Margin="10,5,0,5" >
                            <Run Text="Dormitory management: "  Foreground="DarkBlue" FontWeight="Bold"/>
                            <Run Text="ktx@fpt.edu.vn"/>
                        </TextBlock>
                     </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </DockPanel>
</UserControl>
