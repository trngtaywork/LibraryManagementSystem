<UserControl x:Class="LibraryManagementWpf.View.Student.MyBooks"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Background="#eff2f7">
        <DockPanel DockPanel.Dock="Top" Margin="10">
            <Grid VerticalAlignment="Top" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Margin="0,0,10,0" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Text="Sắp xếp theo" Foreground="#121578"/>

                    <TextBlock Margin="5" VerticalAlignment="Center" Text="Tiêu đề sách:" Foreground="#333"/>
                    <TextBox Width="150" Height="25" Margin="5" 
                 Style="{Binding textboxSearch}"
                 Text="{Binding SearchTextTitle, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock Margin="5" VerticalAlignment="Center" Text="Ngày mượn:" Foreground="#333"/>
                    <ComboBox Width="Auto" Margin="5" SelectedIndex="0"
                  SelectedValue="{Binding SelectedSortBorrowDate, Mode=TwoWay}"
                  SelectedValuePath="Content">
                        <ComboBoxItem Content="Default"/>
                        <ComboBoxItem Content="Last 1 Week"/>
                        <ComboBoxItem Content="Last 1 Month"/>
                        <ComboBoxItem Content="Last 1 Year"/>
                    </ComboBox>

                    <TextBlock Margin="5" VerticalAlignment="Center" Text="Trạng thái:" Foreground="#333"/>
                    <ComboBox Width="Auto" Margin="5" SelectedIndex="0"
                  SelectedValue="{Binding SelectedSortStatus, Mode=TwoWay}"
                  SelectedValuePath="Content">
                        <ComboBoxItem Content="Default"/>
                        <ComboBoxItem Content="Reservation"/>
                        <ComboBoxItem Content="Still Borrowing"/>
                        <ComboBoxItem Content="Lost"/>
                        <ComboBoxItem Content="Return On Time"/>
                        <ComboBoxItem Content="Return Late"/>
                        <ComboBoxItem Content="Cancel"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Border CornerRadius="5" Padding="5" Background="White" Margin="0 10 10 10" Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#dee4ec" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource paginButton}">
                        <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource paginButtonIcon}"/>
                    </Button>

                    <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                    <Button Style="{StaticResource paginButton}"  Background="#7950f2" Foreground="#ffffff">
                        <TextBlock>
                    <Run Text="{Binding CurrentPage}" />
                    <Run Text="/" />
                    <Run Text="{Binding TotalPages}" />
                        </TextBlock>
                    </Button>
                    <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                    <Button Command="{Binding NextPageCommand}" Style="{StaticResource paginButton}">
                        <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource paginButtonIcon}"/>
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>

        <Rectangle DockPanel.Dock="Bottom" Height="2" Fill="#dee4ec" Margin="10,0"/>
        <Rectangle DockPanel.Dock="Top" Height="2" Fill="#dee4ec" Margin="10,0"/>

        <DataGrid ItemsSource="{Binding BorrowedBooksList}" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" Grid.Row="5" x:Name="dataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Book Title" IsReadOnly="True" CanUserResize="False" Width="auto" Binding="{Binding Book.Title}"/>
                <DataGridTextColumn Header="Reservation Date" IsReadOnly="True" Width="*" Binding="{Binding ReservationDate, StringFormat='{}{0:MM/dd/yyyy}'}"/>
                <DataGridTextColumn Header="Borrow Date" IsReadOnly="True" Width="*" Binding="{Binding BorrowDate, StringFormat='{}{0:MM/dd/yyyy}'}"/>
                <DataGridTextColumn Header="Due Date" IsReadOnly="True" Width="*" Binding="{Binding DueDate, StringFormat='{}{0:MM/dd/yyyy}'}"/>
                <DataGridTextColumn Header="Return Date" IsReadOnly="True" Width="*" Binding="{Binding ReturnDate, StringFormat='{}{0:MM/dd/yyyy}'}"/>

                <DataGridTemplateColumn Header="Status" IsReadOnly="True" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StatusString}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Librarian In Charge" IsReadOnly="True" Width="*" Binding="{Binding LibrarianInCharge}"/>

                <DataGridTemplateColumn Header="Operations" IsReadOnly="True" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button ToolTip="View Book Details" Style="{StaticResource gridEditButton}"
                                Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding BookId}">
                                    <Icon:PackIconMaterial Kind="Eye" Style="{StaticResource gridButtonIcon}"/>
                                </Button>
                                <Button Name="Report" Visibility="Collapsed" ToolTip="Report" Style="{StaticResource gridRemoveButton}" Margin="5 0 0 0"
                                Command="{Binding DataContext.ReportBrrowBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding BorrowId}">
                                    <Icon:PackIconMaterial Kind="Alert" Style="{StaticResource gridButtonIcon}"/>
                                </Button>
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="2">
                                    <Setter TargetName="Report" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="3">
                                    <Setter TargetName="Report" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="4">
                                    <Setter TargetName="Report" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="5">
                                    <Setter TargetName="Report" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</UserControl>
