<UserControl x:Class="LibraryManagementWpf.View.Student.ReportList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagementWpf.View.Student"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Background="#eff2f7">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Border CornerRadius="5" Padding="5" Background="White" Margin="0 10 10 10" Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#dee4ec" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource paginButton}">
                        <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource paginButtonIcon}"/>
                    </Button>

                    <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                    <Button Style="{StaticResource paginButton}"  Background="#7950f2" Foreground="#ffffff">
                        <TextBlock>
             <Run Text="{Binding CurrentPage}" />
             <Run Text="/" />
             <Run Text="{Binding TotalPages}" />
                        </TextBlock>
                    </Button>
                    <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                    <Button Command="{Binding NextPageCommand}" Style="{StaticResource paginButton}">
                        <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource paginButtonIcon}"/>
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>

        <Rectangle DockPanel.Dock="Bottom" Height="2" Fill="#dee4ec" Margin="10,0"/>
        <Rectangle DockPanel.Dock="Top" Height="2" Fill="#dee4ec" Margin="10,0"/>
        
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid ItemsSource="{Binding ReportsList}" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" x:Name="dataGrid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Report Reason" IsReadOnly="True" CanUserResize="False" Width="*" Binding="{Binding ReportReason}"/>
                    <!--<DataGridTextColumn Header="Report Type" IsReadOnly="True" Width="*" Binding="{Binding ReportType}"/>-->
                    <DataGridTextColumn Header="Created At" IsReadOnly="True" Width="*" Binding="{Binding CreatedAt, StringFormat='{}{0:MM/dd/yyyy}'}"/>

                    <DataGridTemplateColumn Header="Status" IsReadOnly="True" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Librarian In Charge" IsReadOnly="True" Width="*" Binding="{Binding Borrow.LibrarianInCharge}"/>


                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>
