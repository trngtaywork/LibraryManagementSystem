<UserControl x:Class="LibraryManagementWpf.View.Student.StudentFines"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
              d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Background="#eff2f7">
        <DockPanel DockPanel.Dock="Top" Margin="10">
            <Grid VerticalAlignment="Top" Margin="0,0,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" VerticalAlignment="Top" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Margin="0,0,10,0" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Text="Sắp xếp theo" Foreground="#121578"/>

                        <TextBlock Margin="5" VerticalAlignment="Center" Text="Tiêu đề:" Foreground="#333"/>
                        <TextBox Width="150" Height="25" Margin="5" VerticalAlignment="Center"
                                Style="{Binding textboxSearch}" DockPanel.Dock="Left"  
                                Text="{Binding SearchTextTitle, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Margin="5" VerticalAlignment="Center" Text="Số tiền:" Foreground="#333"/>
                        <ComboBox Width="Auto" Margin="5" SelectedIndex="0"
                   SelectedValue="{Binding SelectedAmountRange, Mode=TwoWay}" 
                 SelectedValuePath="Content">
                            <ComboBoxItem Content="Default"/>
                            <ComboBoxItem Content="0 - 10.000VND"/>
                            <ComboBoxItem Content="10.000 - 50.000VND"/>
                            <ComboBoxItem Content=">50.000VND"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </Grid>
        </DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Border CornerRadius="5" Padding="5" Background="White" Margin="0 10 10 10" Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#dee4ec" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource paginButton}">
                        <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource paginButtonIcon}"/>
                    </Button>

                    <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                    <Button Style="{StaticResource paginButton}"  Background="#7950f2" Foreground="#ffffff">
                        <TextBlock>
                <Run Text="{Binding CurrentPage}" />
                <Run Text="/" />
                <Run Text="{Binding TotalPages}" />
                        </TextBlock>
                    </Button>
                    <Rectangle Width="1" Fill="#dee4ec" Margin="3 7"/>

                    <Button Command="{Binding NextPageCommand}" Style="{StaticResource paginButton}">
                        <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource paginButtonIcon}"/>
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>

        <Rectangle DockPanel.Dock="Bottom" Height="2" Fill="#dee4ec" Margin="10,0"/>
        <Rectangle DockPanel.Dock="Top" Height="2" Fill="#dee4ec" Margin="10,0"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding FinesList}" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" x:Name="dataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Reason" IsReadOnly="True" Width="*" Binding="{Binding FineType}"/>
                <DataGridTextColumn Header="Book Title" IsReadOnly="True" Width="*" CanUserResize="False" Binding="{Binding Borrow.Book.Title}"/>
                <DataGridTextColumn Header="Amount" IsReadOnly="True" Width="*" Binding="{Binding FineAmount, StringFormat='{}{0:N0} ₫'}"/>
                <DataGridTextColumn Header="Librarian In Charge" IsReadOnly="True" Width="*" Binding="{Binding Borrow.LibrarianInCharge}"/>
                <DataGridTemplateColumn Header="Operations" IsReadOnly="True" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="Report" ToolTip="Report" Style="{StaticResource gridRemoveButton}" Margin="5 0 0 0"
                Command="{Binding DataContext.ReportFineCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CommandParameter="{Binding FineId}">
                                <Icon:PackIconMaterial Kind="Alert" Style="{StaticResource gridButtonIcon}"/>
                            </Button>
                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</UserControl>
