<Window x:Class="LibraryManagementWpf.View.Student.StudentMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:LibraryManagementWpf.View.Student"
        mc:Ignorable="d"
        Title="StudentMainWindow" 
        Height="720" Width="1080" WindowState="Maximized" WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow" AllowsTransparency="False" Background="Transparent" DataContext="{StaticResource StudentVM}">
    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border Background="#eff2f7" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="MenuColumn" Width="200"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <!--Left Section-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Button HorizontalAlignment="Left" Margin="20 28 0 35" Style="{StaticResource topButton}" Click="MenuButton_Click">
                    <Icon:PackIconMaterial Kind="Menu" Width="15" Height="15"></Icon:PackIconMaterial>
                </Button>

                <Border Grid.Row="1" Background="#623ed0">
                    <DockPanel>
                        <Border DockPanel.Dock="Top"  HorizontalAlignment="Left" Margin="20 -30 0 0" Width="80" Height="80" Padding="3" Background="White" CornerRadius="100">
                            <Ellipse>
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding CurrentUser.Image}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                        </Border>

                        <!--Menu Titles-->
                        <TextBlock Text="{Binding StudentCode}" Foreground="White" FontSize="22" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center" Margin="5 25 0 10"/>
                        <TextBlock Text="{Binding Email}" Foreground="#d0bfff" FontSize="14" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center"></TextBlock>
                        <!--Menu Separator-->
                        <Separator Margin="25" DockPanel.Dock="Top" Height="0.8" Background="#7e5fdc"/>
                        <!--Menu Buttons-->
                        <StackPanel DockPanel.Dock="Top">
                            <RadioButton Style="{StaticResource menuRadioButon}" Cursor="Hand"
                                         Command="{Binding StudentProfileCommand}" IsChecked="True">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Account" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Bảng tin sinh viên"/>
                                </StackPanel>
                            </RadioButton>
                            
                            <RadioButton Style="{StaticResource menuRadioButon}"
                                Command="{Binding NotifyListCommand}">     
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Bell" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Tin tức"/>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton Style="{StaticResource menuRadioButon}"
                                    Command="{Binding BookListCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="ViewList" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Sách"/>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton Style="{StaticResource menuRadioButon}" Cursor="Hand"
                                         Command="{Binding MyBooksCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Book" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Sách đang mượn"/>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton Style="{StaticResource menuRadioButon}" Cursor="Hand"
                                         Command="{Binding StudentFinesCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="HandCoin" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Tiền phạt"/>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton Style="{StaticResource menuRadioButon}"
                                         Command="{Binding StudentFinesHistoryCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="History" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Lịch sử tiền phạt"/>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton Style="{StaticResource menuRadioButon}" Cursor="Hand"
                                            Command="{Binding ReportsCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="AlertCircle" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Báo cáo"/>
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                        <Button Cursor="Hand"  Command="{Binding LoginViewCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource menuButon}" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" Margin="15 0 15 20">
                            <StackPanel Orientation="Horizontal">
                                <Icon:PackIconMaterial Kind="Logout" Style="{StaticResource menuButtonIcon}"/>
                                <TextBlock Text="Logout"/>
                            </StackPanel>
                        </Button>
                    </DockPanel>
                </Border>
            </Grid>


            <!--Main Section-->
            <Grid Grid.Column="1" Margin="30 20 20 20">

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid HorizontalAlignment="Left">
                    <Button x:Name="btnDisplayMenu" Visibility="Hidden" HorizontalAlignment="Left" Margin="0,28,10,0" Style="{StaticResource topButton}" Click="MenuButton_Click">
                        <Icon:PackIconMaterial Kind="Menu" Width="15" Height="15"/>
                    </Button>
                </Grid>

                <Separator Height="1" Grid.Row="1" Background="#dae2ea" Margin="0 1 0 10"/>
                <ContentControl Grid.Row="2" Margin="10"
                Content="{Binding CurrentView}" Grid.ColumnSpan="2"/>

            </Grid>

        </Grid>
    </Border>
</Window>

