<UserControl x:Class="LibraryManagementWpf.View.Librarian.CategoryManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Classic"
             xmlns:uc="clr-namespace:LibraryManagementWpf.View.Admin"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1080"
             DataContext="{StaticResource cateLibVM}" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Theme/DefaultTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#eff2f7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Grid HorizontalAlignment="Left">
                <Button x:Name="btnDisplayMenu" Visibility="Hidden" HorizontalAlignment="Left" Margin="0,28,10,0" Style="{StaticResource topButton}">
                    <Icon:PackIconMaterial Kind="Menu" Width="15" Height="15"/>
                </Button>
            </Grid>

            <TextBlock Text="Quản lý thể loại" FontSize="32" FontWeight="SemiBold" Foreground="Black" HorizontalAlignment="Left" Margin="50 35 0 0"/>

            <!-- Feedback List Section -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Filter Options -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0 0 30 20">
                    <TextBlock Text="Filter by" Grid.Row="4" FontSize="13" Margin="0 0 20 0" FontWeight="SemiBold" Foreground="#121578" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <ComboBox x:Name="txtFilter" Width="150"
                         VerticalAlignment="Center" Margin="0 5" 
                         Style="{StaticResource ComboBoxFlatStyle}"
                         SelectedValue="{Binding SelectedFilterOption, Mode=TwoWay}">
                        <ComboBox.ItemsSource>
                            <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                <sys:String>All</sys:String>
                                <sys:String>Tên</sys:String>
                                <sys:String>Id</sys:String>
                            </x:Array>
                        </ComboBox.ItemsSource>
                    </ComboBox>

                    <!--Search TextBox-->
                    <Grid Margin="40,0,0,0" Width="300" Grid.Row="4" HorizontalAlignment="Right">
                        <TextBox x:Name="txtSearch" Style="{StaticResource textboxSearch}" Margin="0,0,30,0" Text="{Binding Keyword}"/>

                        <TextBlock Panel.ZIndex="1" Text="Search here ..." 
                           Visibility="{Binding ElementName=txtSearch, Path=Text.IsEmpty, Converter={StaticResource BoolToVis}}" 
                           Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False"/>

                        <Button Command="{Binding SearchCommand}" Style="{x:Null}" Cursor="Hand"
                            VerticalAlignment="Center" HorizontalAlignment="Right"
                            Width="30" Height="30" Margin="0,0,35,0" Background="Transparent" BorderBrush="Transparent">
                            <Icon:PackIconMaterial Kind="Magnify"
                           Foreground="#b0b9c6" Margin="0 0 0 0"/>
                        </Button>
                    </Grid>

                    <!-- Add Button-->
                    <Button Command="{Binding AddCommand}" Style="{StaticResource addButton}" Margin="0 0 20 0">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                            <TextBlock Text="Thêm mới"></TextBlock>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Feedback DataGrid -->
                <Border Grid.Row="1" CornerRadius="20" Padding="20" Background="#ffffff" Margin="50 0 50 0">
                    <DataGrid ItemsSource="{Binding ListCategory}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="*"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding CategoryName}" Width="2*"/>
                            <DataGridTextColumn Header="Created By" Binding="{Binding CreatedBy}" Width="*"/>
                            <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedAt}" Width="*"/>
                            <DataGridTemplateColumn Header="Operations" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button CommandParameter="{Binding}" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Style="{StaticResource gridEditButton}" Margin="5 0 0 0">
                                                <Icon:PackIconMaterial Kind="BookEdit" Style="{StaticResource gridButtonIcon}"/>
                                            </Button>
                                            <Button CommandParameter="{Binding}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Style="{StaticResource gridRemoveButton}" Margin="5 0 0 0">
                                                <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
